# === 本地报告 / 分析产物（非源码，不需入库） ===
broken-images-report.html
local-seo-guide.html
seo-geo-audit.html

# === WorkBuddy 本地缓存 / 项目数据（不入库） ===
.workbuddy/

# === 本地维护脚本（含本机绝对路径，非站点内容，不入库） ===
tools/

# === Apache 配置（Vercel 不读，站点用 vercel.json，非站点内容，不入库） ===
.htaccess
